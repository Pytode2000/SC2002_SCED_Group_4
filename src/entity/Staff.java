/*package entity; 

public abstract class Staff extends User {

    private String staffId;

    // Constructor
    public Staff(String userId, String firstName, String lastName, String staffId) {
        super(userId, firstName, lastName);
        this.staffId = staffId;
    }

    // // Optional: Override toString() method for easy display
    // @Override
    // public String toString() {
    //     return "Staff [Staff ID=" + staffId + ", Name=" + getName() + ", Contact=" + getContactInfo()
    //             + ", Department=" + department + ", Job Title=" + jobTitle + "]";
    // }
}
*/