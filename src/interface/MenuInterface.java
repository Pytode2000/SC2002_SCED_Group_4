
public interface MenuInterface { // havent used.

    // Method to display the menu.
    void displayMenu();

    // Method to handle user's menu choice. Used inside displayMenu().
    void handleMenuOption(int option);
}
